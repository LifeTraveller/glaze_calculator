name: glaze_calculator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7
  open_file: ^3.5.7
  sqflite: ^2.3.0
  sqflite_common_ffi_web: ^0.4.5+1
  path_provider: ^2.1.1
  path: ^1.8.3
  permission_handler: ^11.0.1
  file_picker: ^8.0.0+1
  share_plus: ^10.0.0
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
